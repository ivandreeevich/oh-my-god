# Создаем пустой словарь
my_dict = []
# Получаем список всех методов и атрибутов словаря
methods = (my_dict)

# Выводим список методов

print(dir(my_dict), end=" ")

import random

print(len(dir(random)))